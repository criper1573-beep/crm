# Монтаж CRM — Контекст проекта

## Стек
- Backend: Python, FastAPI, SQLite
- Frontend: HTML, CSS, vanilla JavaScript
- Запуск: uvicorn backend.main:app --reload (из корня проекта)
- База данных: data/crm.db (SQLite)

## Структура
- backend/main.py     — FastAPI роуты
- backend/database.py — работа с SQLite
- backend/models.py   — модель лида
- frontend/           — весь фронтенд
- data/context.txt    — контекст бизнеса для AI-функций

## Что сделано
- [x] Структура проекта
- [x] Модель данных (таблица leads)
- [x] CRUD эндпоинты /api/leads
- [x] Статика фронтенда раздаётся с /
- [x] Авто-резюме из переписки и заметок (GRS AI)
- [x] Генерация ответа клиенту с контекстным файлом
- [x] Автоклассификация статуса по последним сообщениям
- [x] Сохранение состояния при перезагрузке
- [x] Переключатель "Завершил общение"
- [x] Воронка фильтруется по датам аналитики
- [x] Деплой на VPS (nginx + uvicorn + systemd)
- [x] SSL сертификат (Let's Encrypt)
- [x] HTTP Basic Auth через nginx
- [x] Автодеплой через GitHub webhook
- [x] Автобекап базы (cron)

## В процессе — Этап 4 (Интеграции)
- [ ] Телеграм (Telethon) — чтение чатов + отправка сообщений из CRM
- [ ] Авито — новые чаты автоматически → новый лид в CRM
- [ ] Объединённая лента: Авито + Телеграм в одном чате
- [ ] Вкладка Звонки — загрузка аудио, транскрибация (Whisper API)

## Соглашения
- Статусы лида: lead, mql, sql, hot, client, repeat, drain_mql, drain_sql, drain_hot
- Бюджеты: lo (<30к), mid (30-100к), hi (>100к)
- Типы объектов: Квартира, Коммерческая, Дом
