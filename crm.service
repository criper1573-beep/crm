[Unit]
Description=Montazh CRM
After=network.target

[Service]
User=root
WorkingDirectory=/root/crm
Environment="PATH=/root/crm/venv/bin"
ExecStart=/root/crm/venv/bin/uvicorn backend.main:app --host 127.0.0.1 --port 8001
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
